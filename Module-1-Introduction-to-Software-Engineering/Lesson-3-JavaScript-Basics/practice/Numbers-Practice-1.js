
// Declare a variable and assign it to a numeric value
// Double that value and re-assign it to the previous variable
let number = 6;
number *= 2
console.log(number);

// Create two numbers, assign them to variables and add them together
 let x = 2;
 let y = 4;
 console.log(x+y);

// Create three numbers, assign them to variables and subtract the smaller two from the largest
let fNumber = 2;
let scndNumber = 4;
let thrdNumber = 8;
thrdNumber - (fNumber+scndNumber);
console.log(thrdNumber);

// Create two even numbers, assign them to variables and divide one from the other
let input = 4;
let divide = 2;
console.log(input/divide);

// Create two odd numbers, assign them to variables and multiply one from the other
let input2 = 3;
let multiply = 5;
console.log(input * multiply);

// Create a variable and log its remainder, if divided by 3, to the console 
let oddNumber = 13;
console.log(oddNumber%5);

// Answer in plain text, what is modulo operator especially useful for?
console.log("The modulus operator \"\%\" is used to find the remainder from the quotient.")